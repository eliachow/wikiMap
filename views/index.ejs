<!DOCTYPE html>
<html lang="en">

<head>
  <title>🌎WikiMap</title>

  <meta charset="UTF-8" >
  <meta http-equiv="X-UA-Compatible" content="IE=edge" >
  <meta name="viewport" content="width=device-width, initial-scale=1.0" >

  <link rel="stylesheet" href="/vendor/normalize-4.1.1.css" />
  <link rel="stylesheet" href="/vendor/border-box.css" />
  <link rel="stylesheet" href="/styles/main.css" />
  <link rel="stylesheet" href="/styles/layout.css" />

  <script src="/vendor/jquery-3.0.0.js"></script>
  <script defer src="/scripts/app.js"></script>

  <!-- api -->
  <script async src="https://maps.googleapis.com/maps/api/js?key=<%= process.env.API_KEY %>&callback=initMap" async
    defer></script>

</head>

<body>
  <!-- nav bar -->
    <%- include('partials/_header') %>

  <!-- loop through map list and return maps by name -->
    <ul>
      <% for (const map of maps) { %>
        <h4><%= JSON.stringify(map) %></h4>
      <li><%= map.name %></li>

      <!-- add button -->
      <form method="post" action="/profile/1">
        <button type="submit">♥</button>
        <input type="hidden" name="mapID" value="<%= map.id %>">
      </form>

    <% } %>
    </ul>
  

  <!-- map API -->
    <div id="map"></div>

    <script src="../public/scripts/script.js"></script>
</body>

</html>