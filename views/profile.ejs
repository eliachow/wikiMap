<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Profile</title>
</head>
<body>
    <!-- nav bar -->
      <%- include('partials/_header') %>

      <!-- list of maps by creatorID -->
      <ul>
        <% for (const creatorMap of creatorMaps) { %>
        <li><%= creatorMap.name %></li>
        <% } %>
      </ul>
      

      <!-- list of fav maps -->
      <ul>
        <% for (const map of favMaps) { %>
        <li><%= map.name %></li>
        <form method="post" action="/fav_maps/<%=map.user_id%>/delete">
          <input type="hidden" name="mapID" value="<%= map.map_id %>">
          <button type="submit">💔</button>
        </form>
        <% } %>
      </ul>
</body>
</html>